@model ExerciseTest.Models.BookAuthor

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>BookAuthor</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Author full name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Author.FullName)
        </dd>
        <dt class="col-sm-2">
            Book title
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(modelItem => modelItem.Book.Title)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-info" asp-action="Edit" asp-route-id="@Model.Id">Edit</a> 
    <a class="btn btn-success" asp-action="Index">Back to List</a>
</div>
